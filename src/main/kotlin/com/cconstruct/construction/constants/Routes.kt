package com.cconstruct.construction.constants

class Routes {
    companion object {
        const val BASE_PATH = "/api"

        const val USERS = "$BASE_PATH/users"
        const val SITES = "$BASE_PATH/sites"
        const val WORKERS = "$BASE_PATH/workers"
        const val PROGRESSES = "$BASE_PATH/progresses"
        const val EVIDENCES = "$BASE_PATH/evidences"
    }
}